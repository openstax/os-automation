[tox]
envlist = py37
skipsdist = true

[testenv]
deps =-rrequirements.txt
setenv = PYTHONHASHSEED = 0
passenv =
commands =
    python -m pytest {posargs} --flake8 --cov --tr-close-on-complete -v
    coverage html
